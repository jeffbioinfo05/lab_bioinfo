# ============================================================
# INVENTÁRIO ANSIBLE
# Para rodar localmente:     ansible-playbook -i inventory.ini playbook.yml -c local
# Para rodar remotamente:    ansible-playbook -i inventory.ini playbook.yml
# ============================================================

[bioinfo_pc]
localhost ansible_connection=local

# Para PC remoto (ex: PC novo na rede):
# 192.168.1.100 ansible_user=jeff ansible_ssh_private_key_file=~/.ssh/id_rsa
